% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{ik_core}
\alias{ik_core}
\title{Compute core inflation measures}
\usage{
ik_core(
  data,
  method = c("trimmed_mean", "weighted_median", "exclusion"),
  trim = 0.08,
  exclude = NULL,
  date_col = "date",
  item_col = "item",
  change_col = "price_change",
  weight_col = "weight"
)
}
\arguments{
\item{data}{A data.frame containing component-level inflation data.}

\item{method}{Character. One of \code{"trimmed_mean"}, \code{"weighted_median"}, or
\code{"exclusion"}.}

\item{trim}{Numeric. Fraction to trim from each tail (for \code{"trimmed_mean"}
only). Default \code{0.08} (8\% symmetric trim, following the Cleveland Fed).}

\item{exclude}{Character vector. Items to exclude (for \code{"exclusion"} only).}

\item{date_col}{Character. Name of the date column. Default \code{"date"}.}

\item{item_col}{Character. Name of the item/component column. Default
\code{"item"}.}

\item{change_col}{Character. Name of the price change column. Default
\code{"price_change"}.}

\item{weight_col}{Character. Name of the weight column. Default \code{"weight"}.}
}
\value{
An S3 object of class \code{"ik_core"} with elements:
\describe{
\item{core}{data.frame with columns: date, core_inflation.}
\item{method}{Character. The method used.}
\item{trim}{Numeric. The trim fraction (if applicable).}
\item{exclude}{Character vector. Excluded items (if applicable).}
\item{headline}{data.frame with columns: date, headline.}
}
}
\description{
Estimates core (underlying) inflation using one of three standard methods:
trimmed mean, weighted median, or exclusion-based. These measures aim to
strip out transitory price movements and reveal the persistent trend.
}
\examples{
data <- ik_sample_data("components")

# Trimmed mean (default)
core_tm <- ik_core(data, method = "trimmed_mean")
print(core_tm)

# Weighted median
core_wm <- ik_core(data, method = "weighted_median")
print(core_wm)

# Exclusion-based
core_ex <- ik_core(data, method = "exclusion", exclude = c("Food", "Transport"))
print(core_ex)
}
\references{
Bryan, M. F. and Cecchetti, S. G. (1993). "The Consumer Price Index as a
Measure of Inflation." NBER Working Paper No. 4505.

Bryan, M. F. and Cecchetti, S. G. (1994). "Measuring Core Inflation." In
Monetary Policy, University of Chicago Press.
}
