% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion.R
\name{ik_diffusion}
\alias{ik_diffusion}
\title{Compute an inflation diffusion index}
\usage{
ik_diffusion(
  data,
  threshold = c("zero", "mean", "target"),
  target = 0.02,
  weighted = TRUE,
  date_col = "date",
  item_col = "item",
  change_col = "price_change",
  weight_col = "weight"
)
}
\arguments{
\item{data}{A data.frame containing component-level inflation data.}

\item{threshold}{Character. The threshold rule: \code{"zero"} (price_change > 0),
\code{"mean"} (above the cross-sectional mean), or \code{"target"} (above an
annualised target rate).}

\item{target}{Numeric. Annualised inflation target (for \code{"target"} threshold
only). Default \code{0.02} (2\%).}

\item{weighted}{Logical. If \code{TRUE} (default), compute the weighted fraction.
If \code{FALSE}, compute the simple (unweighted) fraction.}

\item{date_col}{Character. Name of the date column. Default \code{"date"}.}

\item{item_col}{Character. Name of the item/component column. Default
\code{"item"}.}

\item{change_col}{Character. Name of the price change column. Default
\code{"price_change"}.}

\item{weight_col}{Character. Name of the weight column. Default \code{"weight"}.}
}
\value{
An S3 object of class \code{"ik_diffusion"} with elements:
\describe{
\item{diffusion}{data.frame with columns: date, diffusion_index.}
\item{threshold}{Character. The threshold type used.}
\item{weighted}{Logical. Whether weights were used.}
}
}
\description{
Measures the breadth of price increases across CPI components. For each
period, computes the (weighted or unweighted) fraction of items with price
changes exceeding a threshold. A diffusion index above 0.5 indicates that
more than half of items are experiencing above-threshold price increases.
}
\examples{
data <- ik_sample_data("components")

# Fraction of items with rising prices
d <- ik_diffusion(data, threshold = "zero")
print(d)
plot(d)
}
