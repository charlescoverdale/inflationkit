% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sticky_flexible.R
\name{ik_sticky_flexible}
\alias{ik_sticky_flexible}
\title{Decompose inflation into sticky and flexible components}
\usage{
ik_sticky_flexible(
  data,
  classification,
  date_col = "date",
  item_col = "item",
  change_col = "price_change",
  weight_col = "weight"
)
}
\arguments{
\item{data}{A data.frame containing component-level inflation data.}

\item{classification}{A named logical vector or a data.frame. If a named
logical vector, names correspond to item names and \code{TRUE} indicates sticky.
If a data.frame, it must have columns \code{item} (character) and \code{sticky}
(logical).}

\item{date_col}{Character. Name of the date column. Default \code{"date"}.}

\item{item_col}{Character. Name of the item/component column. Default
\code{"item"}.}

\item{change_col}{Character. Name of the price change column. Default
\code{"price_change"}.}

\item{weight_col}{Character. Name of the weight column. Default \code{"weight"}.}
}
\value{
An S3 object of class \code{"ik_sticky_flex"} with elements:
\describe{
\item{result}{data.frame with columns: date, sticky, flexible, headline.}
\item{classification}{Named logical vector mapping items to sticky/flexible.}
}
}
\description{
Splits CPI components into sticky-price and flexible-price categories based
on a user-provided classification, then computes separate weighted inflation
measures for each group. This follows the Atlanta Fed methodology.
}
\examples{
data <- ik_sample_data("components")
# Classify items
class_vec <- c(
  Food = FALSE, Housing = TRUE, Transport = FALSE,
  Clothing = FALSE, Health = TRUE, Education = TRUE,
  Communication = TRUE, Recreation = FALSE,
  Restaurants = TRUE, Other = FALSE
)
sf <- ik_sticky_flexible(data, classification = class_vec)
print(sf)
plot(sf)
}
\references{
Bils, M. and Klenow, P. J. (2004). "Some Evidence on the Importance of
Sticky Prices." Journal of Political Economy, 112(5), 947-985.
}
